class Solution:
    def finalString(self, s: str) -> str:
        output = ""
        for i in s:
            if i == "i":
                output = output[-1::-1]
            else:
                output += i
        return output
            
